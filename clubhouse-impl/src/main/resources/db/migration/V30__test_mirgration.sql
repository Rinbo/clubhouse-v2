CREATE TABLE foo
(
    id   BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    name varchar(255),
    CONSTRAINT pk_foo PRIMARY KEY (id)
);

CREATE TABLE bar
(
    id     BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    title  varchar(255),
    foo_id BIGINT,
    CONSTRAINT pk_bar PRIMARY KEY (id)
);

ALTER TABLE bar
    ADD CONSTRAINT FK_BAR_ON_FOO FOREIGN KEY (foo_id) REFERENCES foo (id) ON DELETE CASCADE;